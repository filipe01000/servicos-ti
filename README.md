# Filipe Augusto — Serviços de TI

Página estática pronta para GitHub Pages. Não requer instalação nem compilação.

## Publicar

1. Crie um repositório no GitHub.
2. Envie todo o conteúdo desta pasta para a raiz do repositório, mantendo a pasta `assets`.
3. Em **Settings → Pages**, escolha **Deploy from a branch**, a branch `main` e a pasta `/ (root)`. Salve.
4. Aguarde a publicação e abra o endereço exibido pelo GitHub.

## Configurar seu GitHub

Abra `config.js` e preencha `githubUrl` com o endereço completo do seu perfil, por exemplo `https://github.com/seu-usuario`. O botão só aparece quando houver um endereço HTTPS válido do GitHub.

## Contato e conteúdo

O WhatsApp utilizado é 55 71 99298-4439, conforme o panfleto fornecido. Para trocar, atualize `config.js` e o texto visível em `index.html`. Edite serviços, região e apresentação em `index.html`. As cores e a aparência ficam em `style.css`.

## Pedidos de orçamento

O formulário prepara uma mensagem no WhatsApp. O visitante precisa confirmar o envio lá. Não há banco de dados, envio de e-mail, cálculo automático de preço ou armazenamento dos campos no site. Não publique senhas ou dados de clientes no repositório.

## Prévia social

Título e descrição já estão configurados. Depois de saber o endereço publicado, é possível adicionar `og:url` e uma `og:image` com endereço absoluto.

Fontes do Google Fonts com alternativas locais. A página continua funcionando caso as fontes externas não carreguem.
